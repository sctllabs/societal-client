subscription SubscribeVotesByProposalId($proposalId: String!) {
  councilVoteHistories(
    where: { proposal: { id_eq: $proposalId } }
    limit: 100
  ) {
    id
    approvedVote
    votedNo
    votedYes
    councillor {
      id
    }
  }
}
