subscription SubscribeVotesByProposalId($proposalId: String!) {
  voteHistories(where: { proposal: { id_eq: $proposalId } }, limit: 50) {
    id
    approvedVote
    votedNo
    votedYes
    councillor {
      id
    }
  }
}
