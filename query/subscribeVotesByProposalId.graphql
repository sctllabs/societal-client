subscription SubscribeVotesByProposalId($proposalId: String!) {
  voteHistories(where: { proposal: { id_eq: $proposalId } }) {
    id
    approvedVote
    votedNo
    votedYes
    councillor {
      id
    }
  }
}
